# npm 11 blocks install scripts (postinstall) by default.
# esbuild's postinstall installs its native binary — without it, vite fails
# with "Permission denied". Allow install scripts so the build works on Vercel.
allow-scripts=true
